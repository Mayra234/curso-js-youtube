const name = 'Mayra';
console.log(name);

const firstCharacter = name[0];
console.log(firstCharacter);

const lastPosition = name.length - 1;
const latestCharacter = name[lastPosition];

const secondCharacter = name[1];
const penultimateCharacter = name[name.length - 2];

const fruits = ['Banano', 'Manzana', 'Pera', 'uva', 'Mora'];
const penultimateFruit = fruits[fruits.length - 2];

const lastFruit = fruits;
console.log(lastFruit);
